import 'package:flutter/material.dart';

class SFQPage extends StatelessWidget {
  const SFQPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Мольбы из Корана'),
      ),
      body: Container(),
    );
  }
}
