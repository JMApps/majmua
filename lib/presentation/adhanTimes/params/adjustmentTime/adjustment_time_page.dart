import 'package:flutter/material.dart';

class AdjustmentTimePage extends StatelessWidget {
  const AdjustmentTimePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Корректировка минут'),
      ),
      body: Container(),
    );
  }
}
