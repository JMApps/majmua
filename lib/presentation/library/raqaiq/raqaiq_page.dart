import 'package:flutter/material.dart';

class RaqaiqPage extends StatelessWidget {
  const RaqaiqPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ракъаикъ Къуран'),
      ),
      body: Container(),
    );
  }
}
