import 'package:flutter/material.dart';

class NamesOfPage extends StatelessWidget {
  const NamesOfPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Имена Аллаха'),
      ),
      body: Container(),
    );
  }
}
